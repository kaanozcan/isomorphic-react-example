import React, { Component } from "react"

class ToDos extends Component {
  constructor(config){
    super(config);
  }
  render(){
      return (
        <div>
          <h3>To Do's</h3>
        </div>
      );
  }
}

export default ToDos
